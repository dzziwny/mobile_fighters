import 'package:core/core.dart';

const playerAreaHeight = playerPhoneHeight;

const hpBarHeight = 50.0;

const fullPlayerAreaHeight = playerPhoneHeight + hpBarHeight;
const fullPlayerAreaWidth = 100.0;
